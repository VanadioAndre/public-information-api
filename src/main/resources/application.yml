spring:
  application:
    name: public-information-api

logging:
  level:
    root: INFO
    org.springframework: INFO
    org.hibernate: INFO

server:
  port: ${SERVER_PORT:8086}
  servlet:
    context-path: /api